# Alternative GNU Make workspace makefile autogenerated by Premake

ifndef config
  config=debug_x86_64
endif

ifndef verbose
  SILENT = @
endif

ifeq ($(config),debug_x86_64)

else ifeq ($(config),debug_aarch64)

else ifeq ($(config),release_x86_64)

else ifeq ($(config),release_aarch64)

else
  $(error "invalid configuration $(config)")
endif

PROJECTS := 

.PHONY: all clean help $(PROJECTS) 

all: $(PROJECTS)

clean:

help:
	@echo "Usage: make [config=name] [target]"
	@echo ""
	@echo "CONFIGURATIONS:"
	@echo "  debug_x86_64"
	@echo "  debug_aarch64"
	@echo "  release_x86_64"
	@echo "  release_aarch64"
	@echo ""
	@echo "TARGETS:"
	@echo "   all (default)"
	@echo "   clean"
	@echo ""
	@echo "For more information, see https://github.com/premake/premake-core/wiki"